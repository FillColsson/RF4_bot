# 🎯 Инструкция: Выбор области поплавка

## Проблема: Выбор области не работает

Если окно висит и ничего не происходит, это нормально - просто используй другой метод.

---

## ✅ Что делать:

### Вариант 1: OpenCV селектор (РЕКОМЕНДУЕТСЯ)

1. Запусти бот:
```bash
python main.py
```

2. Нажми кнопку "🎯 Выбрать область"

3. Появится диалог:
```
ДА - OpenCV (более надёжный)
НЕТ - Tkinter (более красивый)
```

4. **Нажми ДА** (OpenCV)

5. Откроется окно с экраном:
```
┌─────────────────────────────┐
│                             │
│    Экран игры              │
│                             │
│    Left Click + Drag        │
│    Space - Save             │
│    ESC - Cancel             │
└─────────────────────────────┘
```

6. **Нарисуй прямоугольник:**
   - Нажми левую кнопку мыши
   - Перетащи вокруг поплавка
   - Отпусти кнопку

7. **Сохрани выбор:**
   - Нажми **SPACE** если подходит
   - Нажми **ESC** если хочешь переделать

### Вариант 2: Tkinter селектор (если OpenCV не работает)

1. Нажми кнопку "🎯 Выбрать область"

2. Нажми **НЕТ** в диалоге

3. Откроется красивое окно

4. Рисуй прямоугольник (левая кнопка + перетащи)

5. Отпусти кнопку - область сохранится

### Вариант 3: Самостоятельно через программу

Если оба селектора не работают, используй утилиту:

```bash
python utils/simple_screen_selector.py
```

Она же автоматически выведет координаты в консоль.

---

## 🐛 Если всё равно не работает:

### Способ 4: Ручной ввод координат

Отредактируй `config.json`:

```json
{
  "detection_region": {
    "x": 850,      // Левый край области
    "y": 350,      // Верхний край области
    "width": 450,  // Ширина области
    "height": 380  // Высота области
  }
}
```

Примерные координаты для 1920x1080:
- Левая часть экрана: x=200-400
- Центр: x=750-950
- Правая часть: x=1400-1600
- Сверху: y=200-400
- В центре: y=400-600

### Способ 5: Через отдельную программу

```bash
# Создай простой тест
python -c "
from utils.simple_screen_selector import select_region_simple
region = select_region_simple()
if region:
    print(f'Сохрани в config.json: {region}')
"
```

---

## ✨ Когда будет готово:

После успешного выбора области:
- ✅ Откроется сообщение "Успешно! ✓"
- ✅ Координаты сохранятся в `config.json`
- ✅ Можно запускать бота (F6)

---

## 📝 Итого:

| Способ | Сложность | Надёжность |
|--------|-----------|-----------|
| OpenCV | ⭐ | ⭐⭐⭐ |
| Tkinter | ⭐⭐ | ⭐⭐ |
| Простая программа | ⭐⭐⭐ | ⭐⭐⭐ |
| Ручной ввод | ⭐⭐⭐⭐⭐ | ⭐ |

**Рекомендуется: OpenCV селектор** - он наиболее надёжный!

---

**Готово? Вот как это выглядит:**

```
python main.py
  ↓
Нажми "🎯 Выбрать область"
  ↓
Появится диалог (выбери ДА/НЕТ)
  ↓
Откроется окно с экраном
  ↓
Нарисуй прямоугольник мышкой
  ↓
Нажми SPACE (сохранить) или ESC (отмена)
  ↓
Готово! ✓
```

🎣
